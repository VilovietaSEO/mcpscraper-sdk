[project]
name = "mcpscraper-memory-sdk"
version = "0.1.0"
description = "Official Python client for the memory.mcpscraper.dev hosted memory API"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31",
    "pydantic>=2",
]

[project.urls]
Repository = "https://github.com/VilovietaSEO/mcpscraper-sdk"

[dependency-groups]
dev = [
    "pytest>=8",
    "responses>=0.25",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mcpscraper_memory"]
